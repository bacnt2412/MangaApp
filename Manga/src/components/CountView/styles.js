import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    alignItems: 'center'
  },
  title: {
    fontWeight: '100',
    fontSize: 14,
    color: '#B5B5B5',
  }
});

export default styles;
